apiVersion: v1
kind: Service
metadata:
  name: book-service
  namespace: book-service
spec:
  selector:
    app: book-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
